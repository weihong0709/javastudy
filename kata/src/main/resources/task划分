1、字符串为空的情况
   分为都为空、或者任何一方为空的情况
2、数字串比较
（1）普通数字串相等
（1）字符串为空，另外一边为数字串，测试为空的情况已经测试
（2）数字串相等，前面包含0，0的数量不一样（三种情况，0的数量等于、小于大于）
（2）数字串不相等，前面包含0，
    大于的情况，0的数量不一样（三种情况，0的数量等于、小于大于）
    小于的情况，0的数量不一样（三种情况，0的数量等于、小于大于）
（3）普通数字串大于，
 (4) 普通数字串小于
（5）数字串包含转义字符的情况（转义后大于等于小于三种情况）
    转义符的位置不一样，在首尾，在中间，在末尾 三种情况
    包含1个、2个、和2个以上转义符的情况

3、字符串比较
   字符串相等
   字符串普通大于
   字符串普通小于
   字符串一个为空，另外不为空
   字符串包含转义字符的情况
     转义符的位置不一样，在首尾，在中间，在末尾 三种情况
     包含1个、2个、和2个以上转义符的情况
     转义符后面数字的个数，1个、2个、3个的情况
4、比较字符串和数字串
   只有一个字符串和数字串




